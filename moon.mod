name = "justjavac/ttl"

version = "0.1.1"

readme = "README.mbt.md"

repository = "https://github.com/justjavac/ttl"

license = "MIT"

keywords = [ "ttl", "cache", "time-to-live", "in-memory" ]

description = "A small in-memory TTL cache for MoonBit"

options(
  source: "src",
)
